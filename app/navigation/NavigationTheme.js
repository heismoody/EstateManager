import { DefaultTheme } from "@react-navigation/native";

export default {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: "#625cbbc2",
        background: "#ffffff"
    }
};